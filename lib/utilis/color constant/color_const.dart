import 'package:flutter/material.dart';

class AppColors {
  // Light mode colors
  static const Color lightPrimaryColor = Color(0xFF36B498);
  static const Color lightBackgroundColor = Colors.white;
  static const Color lightTextColor = Colors.black;
  static const Color lightAccentColor = Color(0xFFB3E5FC);

  // Dark mode colors
  static const Color darkPrimaryColor = Color(0xFF36B498);
  static const Color darkBackgroundColor = Color(0xFF121212);
  static const Color darkTextColor = Colors.white;
  static const Color darkAccentColor = Color(0xFF03A9F4);

  // Shared colors
  static const Color errorColor = Colors.red;
}
